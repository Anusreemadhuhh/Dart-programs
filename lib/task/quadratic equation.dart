import 'dart:io';
import'dart:math';
void main()
{
  double a,b,c;
  double discriminant, root1,root2,realpart,imaginarypart;
   print("Enter the number(a,b,c):");
   a=double.parse(stdin.readLineSync()!);
   b=double.parse(stdin.readLineSync()!);
   c=double.parse(stdin.readLineSync()!);

}